---
export interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`not-prose ${className}`.trim()}>
  <a
    href="/slash/"
    class="inline-block mt-8 text-amber-700 underline decoration-2 decoration-amber-400 underline-offset-4 hover:text-amber-800 hover:decoration-amber-600 transition-colors"
  >
    More slash pages
  </a>
</div>
